{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Preview: Default browser",
      "type": "node",
      "request": "launch",
      "runtimeExecutable": "npx",
      "runtimeArgs": ["serve", ".", "-p", "3000"],
      "cwd": "${workspaceFolder}",
      "console": "integratedTerminal",
      "serverReadyAction": {
        "action": "openExternally",
        "pattern": "Serving|localhost:3000",
        "uriFormat": "http://localhost:3000"
      }
    }
  ]
}
